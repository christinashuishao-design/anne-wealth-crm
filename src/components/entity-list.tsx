import { EmptyState } from "./empty-state";
export function EntityList({columns,rows}:{columns:{key:string;label:string;render?:(row:Record<string,unknown>)=>React.ReactNode}[];rows:Record<string,unknown>[]}){return <div className="bg-white border border-[#e7dece] rounded-2xl overflow-x-auto">{rows.length?<table className="w-full text-sm"><thead className="bg-[#faf7f1]"><tr>{columns.map(c=><th className="text-left p-3" key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr className="border-t" key={String(row.id??i)}>{columns.map(c=><td className="p-4" key={c.key}>{c.render?c.render(row):String(row[c.key]??"—")}</td>)}</tr>)}</tbody></table>:<EmptyState/>}</div>}
